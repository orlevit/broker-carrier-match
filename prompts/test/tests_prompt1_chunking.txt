Now, write a test file with five different scenarios that are different from the example but should still output valid hierarchical chunks (for example, the first line is Header1 but without #). The test file to be part of unitest that when log messages into file instead of  raise error
